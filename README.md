# Student Task Manager

A simple web-based task management application built with Python and Flask. The application allows users to manage tasks through a web interface and is containerized using Docker for consistent deployment.

## Project Structure

```text
student-task-manager/
├── app.py
├── requirements.txt
├── Dockerfile
├── .gitignore
└── templates/
    └── index.html
